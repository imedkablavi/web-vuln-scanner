# Repository Metadata

Recommended GitHub repository metadata for the public project page.

## Description

> Evidence-first web security scanner for authorized targets with scope controls, local regression tests, auth/RBAC verification, and JSON/HTML/SARIF reports.

## Topics

- `security`
- `web-security`
- `vulnerability-scanner`
- `owasp`
- `appsec`
- `sarif`
- `python`
- `authorized-security-testing`

## Social preview

Use a sanitized report-oriented image rather than exploit imagery.

Suggested composition:

- project name and the phrase `Authorized security testing only`;
- a compact findings summary showing severity, confidence and verification status;
- a visible `Local regression corpus` / `Scope enforced` cue;
- no real hostnames, credentials, tokens, cookies, customer data or exploit output.

Recommended canvas: 1280x640 pixels. Generate the source from the local synthetic report only and review it for secrets before uploading through GitHub repository settings.

## License

The repository contains an MIT `LICENSE` file and matching package metadata. If the project owner wants different licensing terms, change both before the next release.
